# Dependencies
node_modules/

# Playwright output
playwright-report/
test-results/
results/
blob-report/

# Auth state (generated at runtime by globalSetup)
.auth/

# Doc verify artifacts
doc-verify/assertions.json
doc-verify/reports/

# Runtime-extension fixture build output (packed/signed artifact + freshly
# generated Ed25519 keypair) — regenerated by `stage.ts` every run, never
# committed.
fixtures/runtime-extension/build/
