---
description: "Open the Breeze GH queue (PRs · Discussions · Issues)"
---

Invoke the `gh-queue` skill via the Skill tool, then follow its workflow: read `queue.md` for current state, cross-check live state for all three types (`gh pr list`, the discussions GraphQL query, `gh issue list` — all on `LanternOps/breeze`), and report what's waiting on me. Ask what to do next (triage round, deep-review a specific PR/discussion/issue, or close out resolved ones).
