# @breeze/extension-api

> **Deprecated:** Use `@breeze/extension-sdk` for new extensions. This v0.1 compatibility adapter remains available for one stable release.

Existing extensions can continue importing the legacy `parseExtensionManifest`, `ExtensionManifest`, `BreezeExtension`, and `ExtensionContext` names during the compatibility window.
