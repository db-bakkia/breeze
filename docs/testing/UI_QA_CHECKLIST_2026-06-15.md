# UI QA Checklist — Integration `qa/integration-2026-06-15`

_Generated 2026-06-15 from code-reading of the 10 merged-for-QA PRs + main-since-v0.70.0. Each item: where to find it, click-path, what confirms success, what to watch for._

Sections: 6 | Total checks: 59

## Billing — Recurring Contracts, Invoice Engine, Product Catalog

- [ ] **Create a new contract** — _Settings > Organizations > [Org Name] > Contracts tab_
  - Steps: Navigate to Settings > Organizations > [Select an organization] → Click the Contracts tab → Click the 'New contract' button (top-right, blue) → Select an Organization from dropdown (pre-filled if accessed from org tab) → Enter a Contract Name (e.g. 'Managed Services — Acme Co') → Select Billing timing: 'In advance' or 'In arrears' → Select Billing cadence: 'Monthly', 'Quarterly', 'Annual', or 'Custom…' → If custom, enter interval between 1-120 months → Set Start date (required, defaults to today) → Optionally set End date → Optionally check 'Auto-issue generated invoices' (otherwise draft) → Optionally add Notes → Click 'Create contract' button
  - ✅ Verify: Toast appears: 'Contract created'; User is navigated to the contract detail view for the new contract; Contract title is displayed; Status badge shows 'Draft'; Contract details reflect entered values (name, cadence, start date, billing timing); Estimated this period panel shows 'Save the contract, then add lines to see an estimate'
  - ⚠️ Watch: Contract name field is required (Save button disabled if blank); Organization is required on create (dropdown validation); Start date is required; Custom interval must be 1-120 months (outside range = Save disabled); Form validation errors (empty org, no name, invalid interval); No toast or silent failure if network error occurs; Dark theme: text contrast in date inputs and selects; Empty state if no organizations available; Missing toast if MFA or auth error on submit
- [ ] **Edit a draft contract and add billing lines** — _Settings > Organizations > [Org Name] > Contracts tab > [Draft Contract]_
  - Steps: From Contracts list, click a Draft status contract row → Verify contract form is auto-populated with existing values → Modify Contract Name, Billing timing, Cadence, or other header fields as needed → Click 'Save changes' button (grey, lower-right) → Scroll down to the 'Add line' section → Select Line type dropdown: 'Flat fee', 'Per device', 'Per seat', or 'Manual quantity' → Enter Description (required, e.g. 'Workstation management') → Enter Unit price (required, e.g. '99.99') → If 'Manual quantity': enter Quantity (e.g. '5') → If 'Per device': optionally select a Site (scopes device count) → If catalog items exist: optionally select 'Link catalog item' → Optionally check 'Taxable' checkbox → Click 'Add line' button → Repeat to add more lines → Click 'Activate contract' button (blue, lower-right, enabled only if >= 1 line)
  - ✅ Verify: Toast: 'Contract saved' after clicking 'Save changes'; Form values update without page reload; Lines table shows all added lines with correct type, description, price, qty, tax status; 'Estimated this period' panel updates with total of flat/manual lines (per_device/per_seat show '+ auto'); Toast: 'Line added' after each Add line click; Line form resets after successful add (Description empty, qty/price reset to defaults); Toast: 'Contract activated' when clicking Activate; Contract status changes to 'Active' after activation; 'Activate contract' button disappears after activation
  - ⚠️ Watch: Description required (Add line button disabled if blank); Unit price required and must be numeric (invalid price = disabled button); Quantity required for 'Manual quantity' lines; Per-device/Per-seat show 'auto' for quantity (resolved at billing time); Activate disabled if no lines (shows hint: 'Add at least one line to activate'); Line removal: Remove button in each row deletes that line after toast 'Line removed'; No toast if network error on save/add line (silent failure risk); Catalog picker only shows if catalog items exist (empty org = no picker); Site dropdown only appears for 'Per device' line type; Quantity field only appears for 'Manual quantity' type; Dark theme contrast on line table and form fields
- [ ] **Activate a contract and view read-only detail** — _Settings > Organizations > [Org Name] > Contracts tab > [Active Contract]_
  - Steps: After a contract is Activated (or click existing Active contract from list) → Verify contract title displays name and green 'Active' status badge → Scroll through contract header to see: Billing timing, Cadence, Start date, End date, Next billing, Auto-issue, Notes → Verify lines table is read-only (no remove buttons, Type/Description/Price/Qty display) → Scroll to Billing history section to see periods and generated invoices → If periods exist with invoices, click 'View invoice' link to navigate to /billing/invoices/[id] → Click 'Edit' button (top-right, grey) to enter edit mode → Modify any header field (name, timing, cadence, start date, end date, auto-issue, notes) → Click 'Save changes' → Verify contract updates and returns to read-only detail view → In summary panel (right side), verify status, cadence, and description
  - ✅ Verify: Contract detail displays all fields: billing timing (advance/arrears), cadence (e.g. 'Monthly'), start date, end date, next billing date, auto-issue (Yes/No), notes; Lines table shows all contract lines with correct Type, Description, Unit price, Qty, Tax status (✓ or —); Per-device/per-seat quantities display as 'auto'; Billing history table shows periods (date ranges), generation dates, and invoice links; Summary panel shows active status badge, cadence, and descriptive text about auto-generation; 'Generate invoice now' button visible and clickable for active contracts; Toast: 'Contract saved' after edit changes; Edit button toggles to 'Done editing' while editing, changes form layout
  - ⚠️ Watch: Read-only mode: line remove buttons absent, form disabled; Edit button only visible on Active contracts (Draft always editable); Summary panel shows hint about auto-generation behavior; 'Hours under contract' stat is placeholder (—) until feature ships; Invoice links in Billing history navigate correctly (link color in primary theme); Next billing date calculation correct based on start date + cadence; Auto-issue field shows 'Yes' or 'No (drafts)' appropriately; No silent failure on save (must toast success); Dark theme: status badge colors visible (green for Active); Empty billing history if no periods generated yet
- [ ] **Pause, resume, or cancel an active contract** — _Settings > Organizations > [Org Name] > Contracts tab > [Active Contract] > Lifecycle section_
  - Steps: Click an Active contract from the list → Scroll to the right-side panel under the status badge → Verify 'Pause' and 'Cancel' buttons appear → Click 'Pause' button → Verify toast: 'Contract paused' → Verify status badge changes to 'Paused' (blue) → Verify 'Resume' and 'Cancel' buttons now appear → Click 'Resume' button → Verify toast: 'Contract resumed' → Verify status badge returns to 'Active' (green) → Click 'Cancel' button → Verify toast: 'Contract cancelled' → Verify status badge changes to 'Cancelled' (red/grey)
  - ✅ Verify: Toast messages appear for each transition (Paused, Resumed, Cancelled); Status badge color reflects state: Active (green), Paused (blue), Cancelled (grey/red); Buttons dynamically update based on status (only valid transitions show); Contract prevents new invoice generation while Paused or Cancelled; Next billing date remains unchanged through pause/resume
  - ⚠️ Watch: Buttons disabled during transition (no double-submit); No toast = silent failure risk (network error); Button text must be 'Pause', 'Resume', 'Cancel' (case sensitive for testid matching); Cancelled contracts show no lifecycle buttons (final state); Expired contracts also show no lifecycle buttons; Dark theme button contrast (Pause/Resume neutral, Cancel destructive red)
- [ ] **Generate an invoice from an active contract manually** — _Settings > Organizations > [Org Name] > Contracts tab > [Active Contract]_
  - Steps: Click an Active contract → Scroll to right panel and verify 'Generate invoice now' button is visible → Click 'Generate invoice now' button → Verify toast: 'Invoice generated' → User should be navigated to /billing/invoices/[new-invoice-id]
  - ✅ Verify: Toast: 'Invoice generated' appears; Navigation occurs automatically to the new invoice; Invoice status is 'Draft' (if auto-issue = false) or 'Sent' (if auto-issue = true); Generated invoice contains contract lines with calculated amounts; Billing history table on contract detail now shows new period entry
  - ⚠️ Watch: Button disabled if contract is not Active (Paused, Cancelled, Expired); Button disabled during generation (loading state); No toast = silent failure (check console for errors); Invoice generation timing: should complete < 5 seconds; Generated invoice must have current date as invoice date; Lines with per_device/per_seat types resolved from live device counts; Contract balance calculation correct (known amounts + auto estimates)
- [ ] **Filter contracts by status and organization** — _Settings > Organizations > [Org Name] > Contracts tab (or standalone /contracts when locked org not set)_
  - Steps: Navigate to Contracts list view (standalone: /contracts, or org tab) → If standalone (not locked org): verify 'Organization' dropdown shows all orgs → If org tab: verify Organization dropdown is hidden (locked to current org) → Click Status dropdown → Select 'Draft' status → Verify table updates to show only Draft contracts → Select 'Active' → Verify table updates to show only Active contracts → Select 'All statuses' → Verify table shows all statuses mixed → Verify URL hash updates with filters (e.g. #status=active&orgId=...)
  - ✅ Verify: Contract rows display with name, organization, status badge (color-coded), cadence, next bill date; Status badges show correct colors: Draft (grey), Active (green), Paused (blue), Cancelled (red), Expired (grey); Clicking a row navigates to contract detail; Filters persist on back/forward navigation (hash-based routing); Empty state message: 'No contracts match these filters' if no results
  - ⚠️ Watch: Loading spinner while fetching contracts; Error state with 'Try again' button if load fails; Organization filter hidden on org-locked view; Status dropdown shows all 5 options: All statuses, Draft, Active, Paused, Cancelled, Expired; Filtering doesn't reload page (React state update); No contracts display if org has none; Dark theme: table rows readable, status badges high contrast; Console errors if fetch fails silently
- [ ] **Create a new invoice (manual entry, not from contract)** — _Operations > Invoices > [New Invoice Button]_
  - Steps: Navigate to Operations > Invoices in main sidebar → Verify invoices list loads with status filters (Draft, Sent, Partially paid, Overdue, Paid, Void) → Click 'New invoice' button (top-right, blue) if visible, or 'Create invoice' action → Select Organization from dropdown (required) → Enter Invoice date (defaults to today) → Optionally enter Invoice number or let system auto-generate → Add invoice lines: select line type, description, quantity, unit price → Click 'Add line' for each line → Optionally set Tax rate and currency → Review 'Estimated total' in summary panel → Click 'Create invoice' or 'Save as draft'
  - ✅ Verify: Invoice creation form opens with required/optional fields; Invoice lines table updates as lines are added; Estimated total displays sum of line amounts (qty × price); Toast: '[Invoice number created]' on successful save; Invoice status is 'Draft' initially; User is navigated to invoice detail view; All entered data persists (no loss on navigation)
  - ⚠️ Watch: Organization required (dropdown validation); Invoice number can be auto-generated if not provided; Date fields must be valid ISO dates; Unit price required and numeric (invalid = disabled Save); Lines table shows type, description, qty, price, tax, and total; Currency and tax rate apply to whole invoice; Empty lines state: 'No lines yet. Add a line below'; Per-device/per-seat lines in manual entry should warn or disable (only for contracts); No silent save failures (must show toast)
- [ ] **Edit a draft invoice and send it** — _/billing/invoices/[id] (Draft status)_
  - Steps: Click a Draft invoice from the list, or navigate to /billing/invoices/[invoice-id] → Verify invoice detail shows: invoice number, date, organization, status (Draft), lines, totals → Click 'Edit' button (if present on Draft) → Modify invoice number, date, or add/remove lines → Click 'Save changes' → Verify toast: 'Invoice saved' → Click 'Send invoice' button (blue, on draft invoices) → Verify toast: 'Invoice sent' → Verify status badge changes to 'Sent' (blue) → Verify 'Send' button disappears and 'Record payment' appears
  - ✅ Verify: Invoice detail displays all fields: number, date, organization, customer/recipient, lines, subtotal, tax, total, balance; Lines table shows: description, quantity, unit price, tax status (Y/N), line total; Edit mode toggles form fields on/off; Toast: 'Invoice saved' on save; Toast: 'Invoice sent' when sending; Status badge updates from 'Draft' to 'Sent'; PDF download link available (eye icon or 'Download PDF'); Sent invoices show 'Record payment' button
  - ⚠️ Watch: Send button only appears on Draft invoices; Edit button only appears on Draft/unsent invoices; Sent invoices become read-only (no edit after send unless voided and reissued); Invoice number required before send (validation); Organization required (cannot change after creation); No silent failures (must show error toast if send fails); Email sending delay: may take 1-2 seconds; Dark theme: status badge colors visible; Empty lines error: cannot send invoice with no lines
- [ ] **Record a payment on an invoice** — _/billing/invoices/[id] (Sent, Overdue, or Partially paid status)_
  - Steps: Click a Sent invoice (status not Draft or Paid) → Verify 'Record payment' form appears in right panel → Enter payment amount (e.g. '500.00') → Select payment method dropdown: 'Bank transfer', 'Credit card', 'Check', 'Other' → Optionally enter reference (e.g. check number, transaction ID) → Verify payment date defaults to today → Click 'Record payment' button → Verify toast: 'Payment recorded' → Verify balance updates (invoice balance decreases) → Verify payment appears in Payments table below with date, amount, method, reference
  - ✅ Verify: Payment form is visible only on Sent/Overdue/Partially paid invoices; Payment amount field is required and numeric; Payment method dropdown shows 4+ options; Payment date defaults to today (can override); Balance calculation correct: new balance = invoice total - sum of payments; Status updates: Partially paid (partial), Paid (full); Payments table lists all recorded payments with correct values; Toast: 'Payment recorded' confirms action
  - ⚠️ Watch: Payment amount > 0 required (disabled if blank or 0); Reference field optional; Cannot record payment if invoice status is 'Draft', 'Void', or 'Paid'; Payment date must be before today or today (no future dates); Multiple payments can be recorded for same invoice; No silent failures (must show error toast); Balance updates instantly (no reload needed); Void payment button appears next to each payment in table; Cannot overpay (if full amount paid, invoice marked 'Paid')
- [ ] **Void an invoice and optionally reissue** — _/billing/invoices/[id] (Sent, Partially paid, or Overdue status)_
  - Steps: Click an invoice (status: Sent, Partially paid, or Overdue) → Scroll to right panel and click 'Void invoice' button (destructive red) → Dialog appears with warning icon and 'Void this invoice?' → Enter void reason in text area (e.g. 'Duplicate issued by mistake') → Optionally check 'Reissue this invoice' to create a new draft copy → Click 'Confirm void' button in dialog → Verify toast: 'Invoice voided' → Verify status badge changes to 'Void' (grey) → If reissue checked, verify new draft invoice created and opened
  - ✅ Verify: Void confirmation dialog appears with warning styling; Void reason is required (Confirm button disabled if blank); Toast: 'Invoice voided' appears; Status badge updates to 'Void' (greyed out); Invoice becomes read-only (no Edit or Send buttons); If reissue checked: new invoice created with same lines and details (Draft status); Void reason is recorded in audit trail (if visible in invoice detail); Payments are preserved in Payments table (not deleted)
  - ⚠️ Watch: Void button only appears on Sent/Partially paid/Overdue invoices; Cannot void Draft or already-Void invoices; Void reason text area required (min 1 char); Reissue checkbox optional (can void without reissuing); Dialog has Cancel button to abort void operation; No silent void (must show toast); Voided invoices exclude from balance calculations; If reissue: new invoice number should be auto-generated (not same as voided); Dark theme: dialog background and button contrast
- [ ] **Download invoice as PDF** — _/billing/invoices/[id] (any status except Draft)_
  - Steps: Click any invoice (Sent, Paid, Overdue, Partially paid, or Void status) → Locate PDF download button/link (eye icon, 'Download', or 'View PDF') → Click PDF download → Verify browser file download dialog appears → Verify filename matches invoice number (e.g. 'INV-001.pdf')
  - ✅ Verify: PDF downloads successfully (no 404 or 500); PDF filename contains invoice number; PDF opens in browser or downloads to Downloads folder; PDF displays: invoice number, date, organization, lines, totals, payment terms; PDF renders all invoice data accurately
  - ⚠️ Watch: Draft invoices show 'Preview only' on PDF (may not allow download); Sent+ invoices allow full download; Filename sanitization: no special chars (replaces with -, _); PDF generation time: should complete < 3 seconds; No 404 if invoice has special chars in number; Void invoices should show watermark 'VOIDED' on PDF; Dark theme: PDF view readable (not stuck in dark theme)
- [ ] **Filter invoices by status, organization, and date range** — _Operations > Invoices_
  - Steps: Navigate to Operations > Invoices → Verify filter section with dropdowns: Organization, Status, Date range (From/To) → Click Status dropdown and select 'Draft' → Verify table updates to show only Draft invoices → Click Organization dropdown and select a specific org → Verify table filters to that org's invoices → Enter 'From' date (e.g. 2026-01-01) → Enter 'To' date (e.g. 2026-06-30) → Verify table shows only invoices within that date range → Verify URL hash updates with filter params (status=draft&orgId=...&from=...&to=...)
  - ✅ Verify: Invoice rows display: invoice number, date, organization, customer, status (color-coded), balance, amount; Status badges are color-coded: Draft (grey), Sent (blue), Partially paid (yellow), Overdue (red), Paid (green), Void (grey); Filters apply simultaneously (org + status + date all respected); Empty state: 'No invoices match these filters' if no results; Filters persist on back/forward navigation (hash-based)
  - ⚠️ Watch: Loading spinner while fetching invoices; Error state with 'Try again' button if load fails; Date filters are inclusive (from ≤ invoice date ≤ to); Organization filter shows all orgs (empty option = all); Status dropdown shows all 6 options: All statuses, Draft, Sent, Partially paid, Overdue, Paid, Void; Date inputs accept ISO format (YYYY-MM-DD); No invoices display if filters have no matches; Clicking invoice row navigates to detail; Dark theme: table rows readable, status badges high contrast
- [ ] **Assemble invoices from a billing period** — _Operations > Invoices > [Assemble invoices dialog]_
  - Steps: Navigate to Operations > Invoices → Look for 'Assemble invoices' button or action (often top-right or in toolbar) → Click 'Assemble invoices' button → Dialog opens: select Organization (required) → Select Site (optional, defaults to all) → Enter 'From' date (start of billing period) → Enter 'To' date (end of billing period) → Click 'Assemble' button → Verify toast: 'Invoices assembled' or similar → Verify new draft invoices created for the period
  - ✅ Verify: Assemble dialog appears with required fields highlighted; Organization dropdown is required; Site dropdown is optional (empty = all sites); Date range is required (both From and To); Toast: '[X] invoices assembled' or similar on success; New draft invoices appear in the list; Assembled invoices contain lines for usage/charges in the period
  - ⚠️ Watch: Organization required (Assemble button disabled if not selected); Date range required (both dates needed); Cannot assemble for dates in future; Assemble button disabled during operation (loading state); No invoices created if period has no charges (may show message); Duplicate prevention: cannot assemble same period twice; No silent failures (must show toast with count or error); Site filter narrows device/charge set (affects assembled amounts); Dark theme: dialog text and buttons visible
- [ ] **Create a product catalog item (hardware, software, or service)** — _Settings > Product Catalog (partner/system accounts only)_
  - Steps: Navigate to Settings > Product Catalog → If org-scope account, verify message: 'The product catalog is available to partner accounts only' → If partner/system scope: click 'Add item' button (top-right, blue) → Catalog item editor form appears → Select Type dropdown: 'Hardware', 'Software', or 'Service' → Enter Name (e.g. 'Microsoft Office 365') → Optionally enter SKU (e.g. 'MSO365-EDU') → Enter Unit price (required, e.g. '99.99') → Optionally enter Cost basis (e.g. '50.00') → Optionally check 'This item is a bundle' → Click 'Save' button → Verify toast: '[Item name] created' → Verify item appears in catalog items table
  - ✅ Verify: Catalog Settings page loads for partner/system users; Item editor form appears with all fields; Type dropdown shows: Hardware, Software, Service; Name field is required (Save disabled if blank); Unit price field is required and numeric (Save disabled if blank/invalid); Cost basis is optional; Bundle checkbox is optional; Toast: '[Item name] created' confirms save; New item appears in table with correct type, name, SKU, price, bundle status
  - ⚠️ Watch: Org-scope accounts see 'partner accounts only' message (cannot access); Name field required (Save button disabled if blank); Unit price required and must be numeric (Save disabled if blank or invalid); SKU optional (can be blank); Cost basis optional (can be blank); Bundle checkbox optional (defaults to unchecked); No silent save failures (must show error toast); Form resets after successful save; Unit price must be positive number; Dark theme: form input contrast and labels readable
- [ ] **Edit a catalog item** — _Settings > Product Catalog > [Edit button on item row]_
  - Steps: Navigate to Settings > Product Catalog → Locate an existing catalog item in the table → Click 'Edit' link/button next to the item → Item editor form appears with current values → Modify any field (Type, Name, SKU, Unit price, Cost basis, Bundle) → Click 'Save' button → Verify toast: 'Item updated' → Verify table row updates with new values
  - ✅ Verify: Edit form loads with all current item values pre-filled; Form appears inline or in a modal; All fields can be modified; Toast: 'Item updated' confirms save; Table row reflects changes immediately (no reload needed); Unit price updates correctly in pricing displays
  - ⚠️ Watch: Name required (Save disabled if blank); Unit price required (Save disabled if blank/invalid); Cannot change item type after creation (may be locked); No silent save failures (must show error); Cancel button closes editor without saving; Form validation shows error if unit price is invalid; Bundle status can be toggled; Dark theme: form contrast and readability
- [ ] **Archive a catalog item** — _Settings > Product Catalog > [Archive button on item row]_
  - Steps: Navigate to Settings > Product Catalog → Locate an existing catalog item in the table → Click 'Archive' link/button next to the item → Verify toast: 'Item archived' → Verify item disappears from the active items table
  - ✅ Verify: Archive button appears next to Edit for each item; Toast: 'Item archived' confirms action; Archived item is removed from the visible list; Archived items are not displayed in create/edit pickers (for contracts, invoices)
  - ⚠️ Watch: Archive button disabled during operation (loading state, may show 'Archiving…'); No confirmation dialog (archive is irreversible, may want to warn); No silent failures (must show error toast if archive fails); Archived items are soft-deleted (can be restored by API if needed); Cannot use archived items in new contracts/invoices; Dark theme: button text visible
- [ ] **Delete a software catalog item with cascade warning** — _Admin > Software Library catalog > [Delete button on item detail]_
  - Steps: Navigate to Software Library catalog → Click on a software item to view detail modal → Locate 'Delete' button (trash icon, red text, bottom-right) → Click 'Delete' button → Confirmation dialog appears with warning icon and 'Delete package?' → Dialog states: 'This removes [Package Name] from the catalog and all associated versions' → Click 'Cancel' to abort deletion → Dialog closes, item remains → Click 'Delete' button again on the item → Dialog reappears → Click 'Delete' button in dialog to confirm → Verify toast: 'Deleted "[Package Name]"' → Verify item is removed from the catalog list → Verify detail modal closes
  - ✅ Verify: Delete button appears only on software items (not other catalog types); Delete confirmation dialog shows warning styling (trash icon, red text); Dialog text clearly explains version cascade: 'This removes [item] from the catalog and all associated versions'; Cancel button closes dialog without deleting; Delete button in dialog triggers deletion; Toast: 'Deleted "[Package Name]"' confirms deletion; Deleted item is immediately removed from list; Modal closes after deletion
  - ⚠️ Watch: Delete button only on active (not archived) items; Delete is irreversible (no undo); Cascades to delete all software versions (test that versions also gone); Cancel path works correctly (dialog closes, item remains); No double-delete (button disabled during deletion, shows 'Deleting...'); No silent failures (must show error toast if delete fails); Cannot delete items in-use (e.g., deployed to devices - may show error); Dark theme: dialog background and button contrast; Audit trail updated with deletion event
- [ ] **Link catalog items to contract lines** — _Settings > Organizations > [Org] > Contracts > [Contract] > Add line section_
  - Steps: Navigate to a Contract (Draft or Active in edit mode) → Scroll to 'Add line' section → If catalog items exist in the system, verify 'Link catalog item' dropdown appears → Click 'Link catalog item' dropdown → Select a catalog item from the list → Verify unit price field auto-populates with catalog item's unit price → Optionally modify description to match or override → Click 'Add line' button → Verify line is added with catalog link (if visible in line table)
  - ✅ Verify: Catalog item picker dropdown appears (only if catalog items exist); Catalog shows non-bundle items only (bundles filtered out); Selecting an item pre-fills the Unit price field; Description can be customized or auto-filled from catalog; Line table shows catalog link (if displayed); Pricing comes from catalog (if modified, shows difference)
  - ⚠️ Watch: Catalog picker only shows if catalog items exist (empty catalog = no picker); Only non-bundle items shown in picker; Unit price auto-fills from catalog (cannot be overridden by default); Description auto-filled from catalog name (can be edited); Bundles excluded from picker; Empty catalog state: no picker appears; Dark theme: dropdown text readable
- [ ] **Create and manage per-organization catalog pricing** — _Settings > Organizations > [Org] > Billing tab (future feature, may not be fully implemented)_
  - Steps: Navigate to Settings > Organizations > [Org Name] → Click 'Billing' tab → Look for 'Per-organization pricing' section (if available) → Verify ability to set org-specific prices for catalog items → Enter custom price for an item → Save changes
  - ✅ Verify: Per-org pricing overrides global catalog prices; Contracts using that org show the custom price
  - ⚠️ Watch: Feature may be placeholder or partially implemented; Per-org pricing may require UI design iteration; Pricing tier management (bulk discounts) not yet in scope

## Breeze Authenticator — Risk-Tiered Step-Up Approvals (Phase 2-4)

- [ ] **Register a browser as an approver device (Windows Hello / Touch ID)** — _Settings > Profile Settings > Approval security section (bottom of page after Passkeys)_
  - Steps: Click Settings (user menu, top-right) → Click Profile Settings → Scroll to 'Approval security' section (after Passkeys section) → In 'Register this device' card, enter a name in 'Device name' field (e.g. 'Front-desk laptop') → Click 'Register this device' button → When prompted, complete Windows Hello/Touch ID/biometric verification on your device → Watch for success toast and the device to appear in the list above
  - ✅ Verify: Device appears in 'Approval security' list with the entered name; Platform-bound badge appears next to device name; Registered date and 'Last used' timestamps are shown (initially empty for last used); Success toast reads: 'This device can now approve requests'; No console errors in DevTools
  - ⚠️ Watch: WebAuthn ceremony timeout/cancel — should surface error message 'Windows Hello verification failed' instead of silently failing; Empty device name defaults to 'This device' label; Offline/network failure during registration — error toast should appear; Dark theme: button contrast and label readability; Platform-bound badge color in dark mode (should use primary/10 background with primary foreground); Register button disabled state while ceremony is in progress ('Registering…' text)
- [ ] **Rename an approver device** — _Settings > Profile Settings > Approval security section_
  - Steps: Register at least one device (see prior test case) → Locate the device row in the list → Click 'Rename' button on the device row → Edit the name in the text input (which auto-focuses) → Click 'Save' button → Wait for success toast
  - ✅ Verify: Device label updates in the list immediately after save; Success toast reads: 'Device renamed'; Rename/Revoke buttons re-appear after save completes; Input and save/cancel buttons disappear once renamed
  - ⚠️ Watch: Empty name field disables the Save button (prevent blank renames); Saving while offline — error toast should appear ('Failed to rename device'); Cancel button aborts the rename without network call; Whitespace-only input should be treated as blank and Save should be disabled; Dark theme contrast on input field
- [ ] **Revoke (disable) an approver device** — _Settings > Profile Settings > Approval security section_
  - Steps: Register at least one device → Locate the device row → Click 'Revoke' button (red/destructive) → Review confirmation dialog: 'Revoke [device name]?' → Click 'Revoke device' button in the dialog (not the X close button) → Wait for success toast
  - ✅ Verify: Confirmation dialog appears with device name in title; Dialog body reads: 'This device can no longer approve requests. You can re-register it at any time.'; Device disappears from the list after revocation; Success toast reads: 'Device revoked'; Device is no longer available for approvals (no longer in allowCredentials for future assertions)
  - ⚠️ Watch: Closing the dialog via X button (onCancel) should NOT revoke — only the 'Revoke device' button should; Revoke button disabled during mutation (shows 'Revoking…' state); Network failure during revoke — error toast and device should still be in list; Dark theme: destructive button color/contrast in dialog; Double-click protection: once revoke is clicked, button should disable and dialog should not accept second clicks
- [ ] **Approve a PAM elevation request with Windows Hello step-up (opt-in L2 proof)** — _Privileged Access > Requests tab_
  - Steps: Navigate to Privileged Access (left sidebar) → Click 'Requests' tab → Locate a pending elevation request in the table (green 'Pending' status) → Click 'Respond' button on the request row → In the modal, keep 'Approve' toggle selected (default) → Set 'Approval window' to 15 minutes (or custom value 1-1440) → Optionally add a 'Reason' (e.g. 'Printer driver update needed') → Click 'Approve elevation' button → When prompted, complete Windows Hello/Touch ID verification (if device is registered) → Wait for success toast and modal close
  - ✅ Verify: Windows Hello/Touch ID prompt appears only if device is registered (optional P2); Request status in list changes to 'Approved' (green badge); Success toast reads: 'Elevation approved'; Modal closes and list refreshes; If device is registered and ceremony completes, the approval should record L2 assurance (webauthn_platform); If no device registered, approval should record L1 assurance (session-tap) without prompting
  - ⚠️ Watch: Silent failures: if assertion ceremony is cancelled (user hits Escape, hardware timeout), error should surface ('Windows Hello verification failed') and submit should NOT occur; No-device case: if approver has no registered device, WebAuthn ceremony should NOT fire — approval should proceed as L1 silently without user prompting for missing biometric; Race condition: if another approver/system actioned the request first, 409 response should show 'Request is not pending' and modal should close; Duration field validation: min=1, max=1440; non-numeric input should be rejected; Dark theme: toggle button colors (green when approve selected, red when deny selected); Reason textarea max-length 2000 characters; Offline during assertion: error should surface and not submit; Console errors: WebAuthn promise rejections, fetch errors should not cause unhandled exceptions
- [ ] **Deny a PAM elevation request (no step-up required)** — _Privileged Access > Requests tab_
  - Steps: Navigate to Privileged Access > Requests tab → Locate a pending elevation request → Click 'Respond' button → Click 'Deny' toggle → Optionally add a 'Reason' (recommended for denials) → Click 'Deny request' button → Wait for success toast
  - ✅ Verify: Request status changes to 'Denied' (red badge); Success toast reads: 'Elevation denied'; No Windows Hello/Touch ID prompt appears (denial does not require step-up); Reason is recorded in audit trail; Modal closes and list refreshes
  - ⚠️ Watch: WebAuthn ceremony should NOT fire for deny decision; Network failure during deny — error toast should surface; Dark theme: toggle colors (red when deny selected); Duration field should disappear when 'Deny' is selected (only show on Approve)
- [ ] **Configure per-tier approval security policy (raise-only enforcement floors)** — _Settings > Organizations > [Org name] > Approval Security tab_
  - Steps: Click Settings (top-right user menu) → Click Organizations (or navigate to /settings/organizations) → Click the organization name to open org settings → Click 'Approval Security' tab → For each risk tier (low, medium, high, critical), review the dropdown → Select a higher assurance level (e.g. change 'low risk' from L1 to L2) → Check the 'Require an enrolled approver device' checkbox (enforce block) → Set 'Enforce from' date to today or a future date (grace window) → Click 'Save' button → Wait for success toast
  - ✅ Verify: All four tiers (low, medium, high, critical) are shown with dropdowns; Level labels match: '1: L1 — session tap', '2: L2 — biometric', '3: L3 — biometric + PIN', '4: L4 — hardware key + PIN'; Dropdown only offers levels >= Breeze floor (raise-only, never lower); Critical tier only offers L4 (its floor); High tier offers L3-L4 only; Success toast reads: 'Approval-security policy saved.'; Reload page — settings persist and reappear in same state; Enforcement date field shows ISO format (YYYY-MM-DD) or date picker
  - ⚠️ Watch: Attempting to set a level below Breeze floor should be prevented (dropdown disables it or server rejects); Empty org ID or org not found — should show error state 'No policy available.'; Network failure during load — should show error state with 'Retry' button; Network failure during save — error toast 'Could not save the approval-security policy.'; Loading state: spinner + 'Loading…' text should appear initially; Dark theme: input field contrast, button colors; Date picker field: ensure it parses ISO correctly and stores as ISO string; Partner-admin-only: verify non-partner-admins cannot access or modify this tab (should see error or redirect); Require enrollment checkbox: toggling it should not require save (it's state-only, persisted on Save); Unsaved changes: clarify whether form shows a 'hasUnsavedChanges' indicator before Save
- [ ] **Filter elevation requests by risk tier in PAM Requests tab** — _Privileged Access > Requests tab_
  - Steps: Navigate to Privileged Access > Requests tab → Locate a pending AI tool action request (should show risk tier badge T1, T2, T3, or T4) → Observe the risk tier badge next to the request target → Use the 'Flow' filter dropdown to select 'AI tool action' → Requests should filter to show only AI tool actions with risk tier badges
  - ✅ Verify: AI tool action requests show a risk tier badge (T1, T2, T3, T4) if riskTier is set; T3-T4 badges have red background (bg-red-500/15 text-red-600) in light theme; T1-T2 badges have amber background (bg-amber-500/15 text-amber-600) in light theme; Badge shows compact format: 'T' + number; Tooltip on badge shows 'Risk tier N'; Non-AI-action requests do not show risk tier badge (UAC intercept, JIT admin); UAC intercept and tech_jit_admin flows have no risk tier display
  - ⚠️ Watch: Dark theme: badge colors should be contrast-appropriate (text-red-400/amber-400 in dark); Missing risk tier in API response (null) — should not render badge at all; T4 (critical) requests should stand out visually with high-contrast red; Hover state for badges should show full tier name in tooltip; Badge should not overflow or break line wrapping in the Target column
- [ ] **Fall back to L1 approval when no approver device is registered** — _Privileged Access > Requests tab (when approver has no registered device)_
  - Steps: As a user with NO registered approver devices, navigate to Privileged Access > Requests → Click 'Respond' on a pending request → Click 'Approve elevation' button → Observe that NO Windows Hello/Touch ID prompt appears → Approval should submit and complete without biometric challenge
  - ✅ Verify: No WebAuthn ceremony is triggered when no device is registered; Approval proceeds silently to L1 (session-tap) assurance level; Success toast 'Elevation approved' appears; No error or warning about missing approver device; Approval completes in normal flow with 15-min window (or entered duration)
  - ⚠️ Watch: Silent L1 fallback should NOT block or error (Phase 2 is opt-in, not enforced); No confusing messages like 'Device not found' or 'Please register a device' in P2; When Phase 4 enforcement activates (enforceFrom date passes), this fallback may change to require enrollment; Console error: 'NoApproverDeviceError' is expected internal behavior, not an unhandled error
- [ ] **Display empty state when no elevation requests exist** — _Privileged Access > Requests tab (when no pending requests)_
  - Steps: Filter requests to show an empty state (e.g. filter by status 'Approved' when none exist, or clear all filters on a fresh org) → Observe the empty state message
  - ✅ Verify: Inbox icon displayed; Heading: 'No elevation requests'; Description with context (e.g. 'Nothing waiting on you. New UAC prompts, JIT admin requests, and AI tool actions queue here.' for pending); Help text with link: 'Not seeing expected requests? UAC capture is controlled per device by Configuration Policies → Privileged Access'; Link navigates to /configuration-policies when clicked
  - ⚠️ Watch: Empty state should not appear while loading (spinner should show first); Empty state message should vary based on filter (different for pending vs other statuses); Link in help text should be underlined and hover-able; Dark theme: text color contrast for muted-foreground elements
- [ ] **Show loading state while fetching elevation requests** — _Privileged Access > Requests tab (on initial load or filter change)_
  - Steps: Navigate to Privileged Access > Requests tab → Observe initial load state → Change the status or flow filter → Watch for loading indicator
  - ✅ Verify: Loading state shows spinner + 'Loading requests…' text; Loading state appears before requests are displayed; Spinner is animated (spinning circle or dots); Once requests load, loading state disappears; Filters respond to changes by triggering a new fetch with loading state
  - ⚠️ Watch: Loading state timeout: if fetch takes > 30s, should show error instead of infinite spinner; Abort on unmount: navigating away should cancel the in-flight request (AbortController); Dark theme: spinner color should be visible against background
- [ ] **Show error state when elevation requests fail to load** — _Privileged Access > Requests tab (on network/API failure)_
  - Steps: Simulate a network error (DevTools throttle/offline) or API error (mock 500 response) → Try to load or filter requests → Observe error message
  - ✅ Verify: Error alert box appears with destructive border and background (border-destructive/40 bg-destructive/10); Error text describes the failure (e.g. 'Failed to load requests (HTTP 500)'); Error is visible and actionable (not hidden or grayed out)
  - ⚠️ Watch: Auth error (401) should redirect to /login, not show error alert; Network error messages should be user-friendly, not raw API responses; Error should not dismiss automatically (user can dismiss or retry via filter change); Dark theme: destructive color should be visible

## Ticketing Phase 4: Email-to-Ticket + Queue UI (PR #1360, #1361, #1362, #1413, #1414)"

- [ ] **Enable/disable email-to-ticket inbound processing** — _Settings > Partner > Ticketing tab > Inbound Email section_
  - Steps: Navigate to /settings/partner or /settings/ticketing (redirects to partner settings) → Click the Ticketing tab in the Partner Settings page → Locate the Inbound email section with heading Turn email addressed to your inbound address into tickets → Toggle the Enable email-to-ticket checkbox to enable or disable the feature
  - ✅ Verify: When enabled, the inbound address field becomes visible and the checkbox shows checked state; When disabled, the checkbox shows unchecked state; A success toast appears: Inbound email settings saved (if domain is configured); The setting persists across page reloads
  - ⚠️ Watch: Check for data-testid=inbound-enabled-toggle element in DOM; Verify MFA-aware error handling when 403 is returned: Could not save inbound email settings your session may need MFA re-verification. Retry.; Ensure silent-failure prevention: mutations use runAction() with no-silent-mutations enrollment; Test with both domain configured and unconfigured states; Dark theme contrast: ensure toggle is visible on both light and dark backgrounds
- [ ] **View and copy inbound email address** — _Settings > Partner > Ticketing tab > Inbound Email section > Inbound address field_
  - Steps: Navigate to /settings/partner#ticketing → In the Inbound email section, check the Inbound address field → If platform domain is configured, the address field will display the resolved email address (readonly) → Click the Copy button next to the address field to copy it to clipboard
  - ✅ Verify: The inbound address field shows a readonly input with the resolved email address (e.g. support@tickets.breeze.example); A success toast appears: Inbound address copied after clicking Copy; The address is actually copied to the clipboard (test with paste operation); Field has data-testid=inbound-address; Copy button has data-testid=inbound-address-copy
  - ⚠️ Watch: If domain is not configured, instead of the address field, you should see: The platform inbound domain isnt configured yet. Contact your administrator. (data-testid=inbound-address-unconfigured); Verify readonly input has bg-muted/30 styling to indicate disabled state; Dark theme: ensure text is legible on both light and dark backgrounds; Test navigator.clipboard availability (older browsers may not support it)
- [ ] **Set default triage organization for email tickets** — _Settings > Partner > Ticketing tab > Inbound Email section > Default triage organization dropdown_
  - Steps: Navigate to /settings/partner#ticketing → In the Inbound email section, locate Default triage organization dropdown → Click the dropdown (id=inbound-triage-org, data-testid=inbound-triage-org) → Select an organization from the list or choose None to clear the selection → The selection saves immediately
  - ✅ Verify: Dropdown populates with all accessible organizations; Selected value persists across page reloads; A success toast appears: Inbound email settings saved; The setting appears in the API response at GET /ticket-config under inbound.defaultTriageOrgId
  - ⚠️ Watch: Label shows (reserved for future use) text indicating feature is planned but not yet active; Verify dropdown is populated from GET /orgs/organizations?limit=100; Check that field is disabled while saving (saving=true state); Empty option value reverts selection to None; Ensure orgs are fetched on card mount via loadOrgs()
- [ ] **Enable/disable autoresponse on email-to-ticket** — _Settings > Partner > Ticketing tab > Inbound Email section > Autoresponse toggle_
  - Steps: Navigate to /settings/partner#ticketing → In the Inbound email section, locate Send an autoresponse acknowledging new email tickets checkbox → Toggle the checkbox to enable or disable autoresponse → The setting saves immediately
  - ✅ Verify: Checkbox state reflects the current config.autoresponderEnabled value; A success toast appears: Inbound email settings saved; When enabled, outbound emails for new email-sourced tickets include Auto-Submitted header; One-per-sender-per-24h rate limiting applied server-side
  - ⚠️ Watch: Verify data-testid=inbound-autoresponder-toggle; Check that the feature degrades closed when TICKETS_INBOUND_DOMAIN env var is unset; Ensure autoresponse is only sent for accepted senders (not quarantined emails); Loop prevention: verify auto-response never replies to Auto-Submitted/Precedence bulk/list messages; Test with both enabled and disabled states across page navigation
- [ ] **View inbound email review queue with parse status** — _Settings > Partner > Ticketing tab > Inbound Email section > Review queue table_
  - Steps: Navigate to /settings/partner#ticketing → Scroll to Review queue section with description Quarantined (unknown sender) and failed inbound emails → The queue displays a table of quarantined and failed email rows → Each row shows: sender address, subject, status badge (quarantined/failed), date/time, and error message (if failed)
  - ✅ Verify: Table has data-testid=inbound-review-queue; Each row has data-testid=inbound-row-{id}; Rows show parseStatus as quarantined or failed in a badge; Failed emails display the error message in red text (text-red-600); Unknown senders show (unknown sender) in the fromAddress column; Missing subjects show (no subject) as fallback text; Timestamps display in localized date string format
  - ⚠️ Watch: Verify queue only shows rows with parse_status IN (quarantined, failed); Queue is admin-gated server-side (403 shows message: The review queue is available to admins only. with data-testid=inbound-review-forbidden); Empty queue shows: Nothing to review. (data-testid=inbound-review-empty); Pagination appears when total > 50 rows (PAGE_SIZE=50); Verify fetched from GET /ticket-config/email-inbound?page={p}&limit=50
- [ ] **Convert quarantined/failed email to ticket** — _Settings > Partner > Ticketing tab > Inbound Email section > Review queue > Convert to ticket action_
  - Steps: Navigate to /settings/partner#ticketing → In the Review queue table, locate an email row (quarantined or failed) → Click Convert to ticket link on the right side of the row (data-testid=inbound-convert-{id}) → An inline form appears with organization picker and buttons → Select the target organization from the dropdown (data-testid=inbound-convert-org-{id}) → Click Create ticket button (data-testid=inbound-convert-submit-{id}) → The email is converted and row disappears from the queue
  - ✅ Verify: Clicking Convert to ticket shows inline form with data-testid=inbound-convert-form-{id}; Form contains organization dropdown populated from the orgs list; Default organization pre-selected from cfg.defaultTriageOrgId; Create ticket button is disabled until an org is selected (disabled:opacity-50); Success toast: Ticket created from email; Row is removed from the queue after conversion; New ticket created with source:email in the organization
  - ⚠️ Watch: Validate org selection is mandatory: Pick an organization first. error toast if submitted without selection; Verify POST /ticket-config/email-inbound/{id}/convert with body {orgId} creates ticket; Check for FRIENDLY_CODES error handling: ORG_NOT_ACCESSIBLE, INBOUND_ROW_NOT_FOUND, INBOUND_ROW_ALREADY_RESOLVED, INBOUND_ROW_NO_SENDER; Empty sender block prevents creation (returns 400, This email has no usable sender address...); Idempotency guard: already-converted row returns 409 and message already handled. Refreshing the list.; Verify org-in-partner check server-side (not accessible org returns ORG_NOT_ACCESSIBLE); Cross-tenant isolation: forged convert requests to another tenant's rows return 404
- [ ] **Dismiss quarantined/failed email from review queue** — _Settings > Partner > Ticketing tab > Inbound Email section > Review queue > Dismiss action_
  - Steps: Navigate to /settings/partner#ticketing → In the Review queue table, locate an email row → Click Dismiss link on the right side of the row (data-testid=inbound-dismiss-{id}) → The email is dismissed and removed from the queue → Success toast: Inbound email dismissed
  - ✅ Verify: Row disappears from the queue after dismissal; Success toast appears: Inbound email dismissed; Dismissed row is marked terminal ignored status in database; Verify PATCH /ticket-config/email-inbound/{id}/dismiss called; Row doesn't reappear after page refresh
  - ⚠️ Watch: Check for silent-failure prevention: mutation enrolled in runAction with errorFallback; Idempotency: re-dismissing already-dismissed row should handle gracefully (409 re-loads queue); Verify data-testid=inbound-dismiss-{id}; Dismissed status marked as ignored server-side; Cross-tenant isolation: forged dismiss requests return 404
- [ ] **Pagination in email review queue** — _Settings > Partner > Ticketing tab > Inbound Email section > Review queue pagination controls_
  - Steps: Navigate to /settings/partner#ticketing → If the queue has more than 50 emails, pagination controls appear at the bottom of the table → Click Prev button to go to previous page (data-testid=inbound-page-prev) → Click Next button to go to next page (data-testid=inbound-page-next) → Page number displayed in center (e.g., Page 1 of 3)
  - ✅ Verify: Pagination controls have data-testid=inbound-pagination; Prev button disabled on page 1 (disabled:opacity-40); Next button disabled on last page (disabled:opacity-40); Clicking pagination buttons reloads queue with new page parameter; Page display shows Page {page} of {totalPages} format
  - ⚠️ Watch: PAGE_SIZE constant is 50 rows per page; totalPages calculated as Math.ceil(total / PAGE_SIZE); Pagination only shows when totalPages > 1; Verify goPage() function calls loadQueue(p); Test with exactly 50, 51, 100, 101 emails to verify boundary conditions
- [ ] **Load inbound email card with error handling** — _Settings > Partner > Ticketing tab > Inbound Email section > Card initialization_
  - Steps: Navigate to /settings/partner#ticketing → Wait for the Inbound Email card to load → Verify all sections appear (settings + review queue)
  - ✅ Verify: Card has data-testid=inbound-email-card; During load, displays Loading. message (data-testid=inbound-email-loading); After successful load, all sections are visible; Config, orgs, and queue load in parallel via loadAll()
  - ⚠️ Watch: If any fetch fails, displays Inbound email settings failed to load. with Retry button (data-testid=inbound-email-error and inbound-email-retry); Queue-specific 403 doesn't break the card: queue hides with message The review queue is available to admins only.; Verify three parallel fetches: GET /ticket-config, GET /ticket-config/email-inbound, GET /orgs/organizations?limit=100; UNAUTHORIZED (401) redirects to login with returnUrl; Test with network failure, 403, 404, 500 responses
- [ ] **Ticket assignee dropdown with dark theme fix** — _Tickets > Queue > Workbench > Assignee select_
  - Steps: Navigate to /tickets → Click on a ticket in the queue to open the workbench pane → Locate the assignee dropdown in the ticket details panel (data-testid=ticket-workbench-assignee) → Click the dropdown to open the assignee list → Select a user or Unassigned option → Test with both light and dark themes
  - ✅ Verify: Dropdown displays user names and emails from the assignee list; Selected assignee appears in the input field; Assignee list updates when selection changes; In dark theme, text is readable (uses text-foreground class)
  - ⚠️ Watch: Fixed commit fix(ticketing): theme-aware assignee select (was white on dark theme) changed class from px-2 py-1 to bg-background px-2 py-1 text-xs text-foreground; Verify dropdown has explicit bg-background (not default muted); Text color uses text-foreground (not white/default); Test on dark theme to ensure white text on dark background is fixed; Verify in both light and dark CSS custom properties
- [ ] **Ticket queue UI without per-action refetch cascade** — _Tickets > Queue > All actions (assign, status change, bulk operations)_
  - Steps: Navigate to /tickets tab → Select a ticket from the queue → Change ticket status or assignee in the workbench pane → Change another ticket's status/assignee → Perform bulk operations on multiple tickets → Observe network requests in browser DevTools
  - ✅ Verify: After each action, only one background /tickets refetch occurs (not one per action); List is updated with instant patchTicketRow() optimistic updates; Stats are refreshed via background scheduleReconcile() after 300ms debounce; No redundant per-action API calls to /tickets
  - ⚠️ Watch: Commit perf(ticketing): kill the per-action refetch cascade in the queue UI eliminated N+1 query problem; Verify patchTicketRow() updates list instantly without fetch; scheduleReconcile() coalesces burst of mutations into single refresh (300ms debounce); Test with rapid status/assignee changes to confirm coalescing; Monitor console for duplicate /tickets requests; Check reconcileTimer cancellation logic prevents stale timeouts
- [ ] **Inbound Email tab gating by partner scope** — _Settings > Partner > Ticketing tab > Inbound Email tab_
  - Steps: Log in as a partner-scoped user → Navigate to /settings/partner#ticketing → Verify Inbound Email tab appears in the tab list (data-testid=ticketing-tab-inbound) → Click the tab to view inbound email settings → Log in as an organization-scoped user → Navigate to /settings/partner (or /settings/ticketing) → Verify Inbound Email tab does NOT appear
  - ✅ Verify: Partner-scoped users see tab with data-testid=ticketing-tab-inbound; Organization-scoped users do not see the tab at all; Tab can be deep-linked via /settings/partner#tab=inbound for partner users; Tab panel has data-testid=ticketing-tab-panel-inbound
  - ⚠️ Watch: Client-side gating: canManageInbound = getJwtClaims().scope === partner; Server-side admin gate: queue routes (list/convert/dismiss) use adminMiddleware; Card's 403 handler is defense-in-depth: queue hides for non-admins; JWT scope is not a permission system; server enforces with adminMiddleware; Test with forged JWT claiming partner scope (should 403 on queue routes)
- [ ] **Inbound Email tab within Partner Settings nested subtabs** — _Settings > Partner > Ticketing tab > [Inbound Email / Statuses / Priorities / Categories / Export subtabs]_
  - Steps: Navigate to /settings/partner#ticketing → Verify TicketingSettingsTabs component renders with 5 subtabs (data-testid=ticketing-settings-tabs) → See subtabs: Statuses, Priorities, Categories, Export, Inbound Email (for partner users) → Click Inbound Email subtab (data-testid=ticketing-tab-inbound) → InboundEmailCard renders with data-testid=ticketing-tab-panel-inbound
  - ✅ Verify: Each subtab has data-testid=ticketing-tab-{id}; Active subtab has border-b-2 border-primary styling; Subtab panel renders with data-testid=ticketing-tab-panel-{id}; Subtab selection persists in URL hash #tab=inbound
  - ⚠️ Watch: syncHash parameter: PartnerSettingsPage passes syncHash={false} to TicketingSettingsTabs to avoid hash collision with parent tab hash; Nested InboundEmailCard keeps its own state independent of parent tab system; Reload /settings/partner#ticketing&tab=inbound should land on inbound subtab (hash parsed correctly); Test subtab switching doesn't affect parent tab selection

## Devices — Unified List, Sortable Columns, Process Drill-Down, OS Labels, Watchdog, Activity Feed

- [ ] **Unified Devices list with network devices (Class column + All/Agent/Network filter)** — _Devices > Devices (Main list, /devices)_
  - Steps: Navigate to the Devices section via the sidebar → Verify that agent devices and network devices (if present) appear in a single table → Look for the Class column header with sortable indicator (arrow icon) → If network devices exist in the fleet, locate the "All / Agent / Network" class filter buttons above the table → Click the "Network" filter button to show only network-discovered devices → Verify network device rows display "Network" badge with network icon in the Class column → Verify agent device rows display "Agent" badge with CPU icon in the Class column → Click "Agent" filter to show only enrolled agents → Click "All" to return to the full view
  - ✅ Verify: Class column is visible in the default column set; Class column header shows sort chevron icon (up/down when sorted, faded ArrowUpDown when not active); Network device rows have a blue "Network" badge with network icon; Agent device rows have a blue "Agent" badge with CPU icon; Class filter buttons (All/Agent/Network) only appear when network devices are present; Clicking a filter immediately updates the visible row count; Type column shows asset type (e.g., 'printer', 'router') for network rows and device role for agent rows
  - ⚠️ Watch: Network device rows appearing with missing Class badge (regression in #1356); Class filter buttons showing when only agent devices exist (should be hidden); Sorting by Class not working or rows not reordering (test numeric/string sort); Dark theme contrast on Class badges (info/15 and info/30 classes may have contrast issues); Network devices appearing in bulk actions marked 'agent-only' (they should be skipped with a warning toast); Empty state when filtering to 'Network' if no network devices exist; Performance lag when fleet has 1000+ devices while sorting by Class
- [ ] **Sortable column headers (every Devices list column clickable)** — _Devices > Devices (Main list, /devices)_
  - Steps: Open the Devices list and verify all column headers display a sort icon (chevron or ArrowUpDown) → Click on the 'Hostname' column header to sort devices alphabetically → Verify an up/down chevron appears in the header indicating sort direction → Click 'Hostname' again to reverse sort (descending) → Click on the 'CPU %' column header to sort by CPU usage (highest first) → Click on the 'OS' column header to sort by operating system → Click on the 'Status' column header and verify devices are ordered by status rank (Online > Updating > Pending > Maintenance > Quarantined > Offline > Decommissioned) → Click on the 'Last Seen' column header to sort by most/least recently seen → Click on the 'Class' column header to sort agent vs. network → Verify blank cells (dashes like offline devices' CPU/RAM) always sort to the bottom regardless of sort direction
  - ✅ Verify: Each column header is clickable and shows a tooltip on hover; Sort icon updates immediately (chevron up/down appears); Rows reorder correctly within 1-2 seconds (no lag for <5000 devices); Blank cells (dashes/em-dashes) always appear at the bottom of the sort; Status column uses semantic ranking, not alphabetic (Online before Offline); Version columns (Agent Version, Watchdog Version) sort using numeric comparison (0.9.x < 0.10.x); Hostname column uses case-insensitive string sort with numeric collation (host-2 < host-10); Sort state persists while filtering (sort order remains when you toggle filters); Sorting works across pagination (sort applies to entire filtered set, not just current page)
  - ⚠️ Watch: Clicking a column header more than once causing sort to get stuck (toggle not working); Blank cells appearing in the middle of the sort (regression in #1284); Hostname sort treating numeric parts as text (host-10 sorting before host-2 indicates no numeric collation); CPU/RAM columns showing '0%' for offline devices when they should show dash and sort to bottom; Agent Version column showing '0.10.x' before '0.9.x' (indicates string sort instead of numeric); Sort icon not changing when clicking header (visual feedback missing); Console errors when sorting a column with special characters in data (tags, org names); Performance degradation when sorting a fleet with 10000+ devices; Dark theme: sort icons difficult to see (check contrast of ArrowUpDown icon)
- [ ] **Process-level resource drill-down (CPU + RAM charts clickable to show top processes)** — _Device Details > Performance tab (/devices/:id)_
  - Steps: Navigate to any online device's detail page → Click on the 'Performance Graphs' or 'Metrics' tab → Locate the CPU/RAM line chart displaying resource usage over time → Click on any data point on the chart (line intersection) to open the process drill-down panel → Verify a dialog appears titled 'Top processes' → In the dialog, verify a table shows: Process name, PID, CPU %, RAM (MB) → Click the 'CPU' sort button at the top of the dialog to sort processes by CPU descending → Click the 'RAM' sort button to switch to sorting by RAM descending → Toggle the 'Live' checkbox to switch from the sampled time to live process listing → When Live is ON, verify the sample time shows 'Live (now)' instead of a timestamp → Close the dialog (click outside or press Escape) and click another chart point to verify drill-down refreshes
  - ✅ Verify: Clicking any data point on the CPU/RAM chart opens a dialog (not a modal overlay that blocks interaction); Dialog shows the nearest process sample captured near the clicked timestamp; Top 16 processes appear (max limit per #1359 design); Processes are sorted by CPU usage by default (highest CPU first); CPU and RAM buttons toggle between the two sort modes; Live toggle fetches fresh process list from /system-tools/devices/:id/processes; When Live is OFF, sample time displays formatted date + time of captured sample; Dialog title shows 'Top processes' with no ambiguity; Process names, PIDs, CPU %, and RAM values render correctly (no NaN or truncation)
  - ⚠️ Watch: Clicking chart does nothing or shows console error (onclick handler may not be wired); Dialog opening but showing 'No sample near this time' when samples exist (timestamp mismatch issue); Process names truncated or missing (wide process names like 'C:\Windows\System32\long_process.exe' should not overflow); CPU % shown as '0.0' for all processes when there's actually CPU activity (sample data stale or wrong); RAM values in MB vs GB confusion (spec says MB, verify units displayed correctly); Live toggle fetches old data instead of real-time (caching issue on /system-tools endpoint); Process list empty when device is online and should have processes; Dialog not closing when clicking outside (backdrop click handler missing); Performance issue: opening drill-down on device with 200+ devices in fleet causes lag; Dark theme: dialog background contrast issues, text unreadable
- [ ] **Normalized device OS labels (Windows, macOS, Linux with distro names properly cased)** — _Devices > Devices (Main list, /devices) + Device Details tab_
  - Steps: Open the Devices list and verify the OS and OS Version columns are visible → Locate a Windows device in the list and verify the OS Version shows 'Windows 11' or 'Windows 10' (not 'windows 11' or raw build strings) → Locate a macOS device and verify the OS Version shows 'macOS 14' or 'macOS Sonoma' (not 'darwin' or 'Darwin 23') → Locate a Linux device and verify the OS Version shows proper distro name (e.g., 'Ubuntu 24.04', 'CentOS 7', 'Fedora 40') with correct capitalization → Check a device with lowercase distro names (e.g., 'ubuntu', 'centos') and verify they render with proper casing → Check an unfamiliar Linux distro and verify it falls back to title-cased name (e.g., 'My Distro' not 'MY DISTRO') → Open a device detail page > Info tab and verify OS Version displays the same normalized format → Sort the list by OS Version column and verify sort order is consistent (numeric versions sort correctly: 11 > 10, 24.04 > 20.04)
  - ✅ Verify: OS Version column shows 'Windows' prefix for Windows versions (not blank or raw build); macOS versions show 'macOS' prefix (not 'Darwin' or 'OS X'); Linux distros show proper casing (Ubuntu, Fedora, CentOS, Pop!_OS, Raspberry Pi OS, etc.); Special distributions handled correctly: AlmaLinux, Rocky Linux, openSUSE, TrueNAS SCALE, Proxmox VE; OS Version sorts numerically (11 before 10, 24.04 before 20.04); No leading/trailing whitespace in OS Version cell text; Hover title attribute shows the same normalized string
  - ⚠️ Watch: OS Version showing raw/lowercase distro names (indicates #1385 normalization not applied); Windows versions missing 'Windows' prefix or showing only build number; macOS showing 'darwin' or 'Darwin' (should never appear to user in new code); Pop!_OS showing as 'Pop OS' or 'Pop!OS' (underscore and exclamation mark handling); Unknown Linux distros falling back to all-caps or all-lowercase (should be title-cased); OS Version column not sortable by clicking header (regression in #1284); Performance issue when list has 200+ devices with varying distro names; Dark theme: unreadable text in OS Version cells (contrast issue)
- [ ] **Watchdog Version column (new sortable column showing agent watchdog version)** — _Devices > Devices (Main list, /devices) + Device Details > Info tab_
  - Steps: Open the Devices list → Locate the column visibility/columns menu button (usually a grid or columns icon in the table header) → Click to open the column selector dropdown → Verify 'Watchdog Version' appears in the list of available columns → Check the checkbox next to 'Watchdog Version' to make the column visible → Verify the Watchdog Version column appears in the table with version numbers (e.g., '1.2.3') or 'N/A' for devices without watchdog → Click the 'Watchdog Version' column header to sort by watchdog version → Verify devices with watchdog versions sort correctly (1.2.3 < 1.10.0 using numeric sort) → Verify agent devices without watchdog show 'N/A' or empty (not '0' or blank dash) → Verify network-discovered devices show 'N/A' (they have no agent/watchdog) → Open a device detail page > Info tab and verify Watchdog Version displays in the device info panel
  - ✅ Verify: Watchdog Version column appears in column selector when available; Column is not visible by default (check DEFAULT_VISIBLE_COLUMNS in code); Enabling column saves preference to localStorage (refreshing page keeps column visible); Watchdog Version header is clickable with sort icon (up/down chevron); Devices with watchdog show version like '1.2.3', '2.0.0'; Devices without watchdog show 'N/A' (not blank or dash); Network devices show 'N/A' in Watchdog Version column; Sort order is numeric (1.10.0 after 1.2.0, not before); Watchdog Version column appears in default column list (COLUMN_IDS) in columnVisibility.ts; Device Info tab shows watchdog version with proper label 'Watchdog Version'
  - ⚠️ Watch: Watchdog Version column not appearing in column selector (regression in #1390); Column showing '0' instead of 'N/A' for devices without watchdog; Watchdog Version sorting as string instead of numeric (1.10.0 before 1.2.0 is wrong); Network device rows showing watchdog version when they shouldn't (should be 'N/A'); Watchdog version truncated or cut off in narrow screens; Column selector not persisting Watchdog Version visibility after page refresh; Info tab showing watchdog version but column header label differs (inconsistency); Dark theme: text in Watchdog Version cells hard to read (contrast issue); Performance: sorting 10000+ devices by Watchdog Version causes lag
- [ ] **Device activity feed on overview tab (deliberate actions like reboot, script, patch, software, maintenance)** — _Device Details > Overview tab (/devices/:id)_
  - Steps: Navigate to any device's detail page (click a device from the list) → Verify the Overview tab is active (default tab) → Scroll down to find the 'Activity' or 'Activity Feed' section → Verify a timeline shows recent device actions (reboot, script execution, patch install, software deploy, etc.) → Each activity row should show: action icon, action name, timestamp, and actor/result → Click on an activity row to see if it expands with more details (if expandable) → Verify deliberate actions (device.command.*, script.*, device.patch.*, device.software.*, device.maintenance.*) appear → Verify non-deliberate actions (like config churn, discovery, monitoring) do NOT appear in overview feed → Verify actor information shows who triggered the action (user name, 'Automation', 'Policy', 'AI', etc.) → Verify timestamps show relative time (e.g., '2m ago') with absolute time on hover
  - ✅ Verify: Activity section appears on device overview (not just on Activities tab); Timeline shows up to 8 activity rows by default (limit parameter in code = 8); Each row has an icon matching the action type (Power icon for reboot, Terminal for script, etc.); Action names are human-readable (not raw enum values like 'device.command.reboot'); Timestamps are relative ('just now', '5m ago', '2h ago', '1d ago'); Hover on timestamp shows absolute time (full date + time); Actor information shows the triggering user or system (name, automation label, etc.); Result status shown if available (success/failure indicator); Feed is read-only on overview (no edit/delete actions visible)
  - ⚠️ Watch: Activity feed missing entirely from overview (regression in #1306); Showing raw action enum values instead of human-readable names; Showing 'just now' for all recent events (timestamp formatting broken); Including monitoring/config events that should be excluded (should filter by ACTION_RULES prefix); Actor showing as blank or 'null' when a user triggered the action; Timestamps truncated or overlapping in narrow viewports; Very long activity names (like full command strings) overflowing or wrapping poorly; No indication when actor is an automation/policy (context missing); Performance: 50+ activities taking more than 1 second to render; Dark theme: activity icons or text difficult to see (contrast issue)

## Patches — Selectable Page Size, Clickable Sorting, Ring Auto-Approval Gate

- [ ] **Selectable page size for patches table** — _Patch Management > Patches tab > Bottom pagination area_
  - Steps: Navigate to sidebar > Patches (Download icon) → Click the 'Patches' tab to view the patches list → Scroll to the bottom of the table to see pagination controls → Locate the 'Rows per page' dropdown selector showing options: 25, 50, 100, 200 → Click the dropdown and select '50' → Observe the table refreshes with 50 rows instead of the default 25 → Verify the page counter updates (should now show 'Page 1 of X' with fewer total pages) → Click next page button, then change page size to '200' → Verify the page resets to 'Page 1' after the page size changes
  - ✅ Verify: Dropdown shows four size options: 25, 50, 100, 200; Default selection is 25 rows per page; Changing the page size immediately updates the visible table rows; Page counter recalculates when page size changes (e.g. 60 patches = 3 pages at 25-per-page, 1 page at 100-per-page); Page always resets to 1 when page size is changed; No console errors when changing page size; Dark theme: dropdown and options are readable with sufficient contrast
  - ⚠️ Watch: Empty state when all 200 patches are filtered out; Page size selector appears even when fewer patches exist than the selected size; Silent failures if the page size value doesn't persist in the dropdown; Layout shift when switching from 25 rows (short table) to 200 rows (tall table); Keyboard accessibility: ensure focus remains on selector after change; Organizations with >200 total patches: only the first 200 loaded from API are paginated/sorted
- [ ] **Clickable, sortable column headers in patches table** — _Patch Management > Patches tab > Table header row_
  - Steps: Navigate to sidebar > Patches → Click the 'Patches' tab → Observe the table header with columns: Patch, Severity, Source, OS, Release, Approval, Actions → Click the 'Patch' column header → Verify the table sorts alphabetically ascending (A-Z) → Click 'Patch' header again to toggle to descending (Z-A) → Click 'Severity' header to sort by severity priority (Critical first, then Important, Moderate, Low) → Click 'Approval' header to sort by approval status (Pending, Deferred, Approved, Declined) → Verify the header shows a visual indicator (up/down chevron) for the active sort → Change to 50 rows per page, navigate to page 2, then click a header to sort → Verify the page resets to page 1 after sorting
  - ✅ Verify: All six sortable headers are clickable buttons; Active sort shows a filled chevron icon (up for asc, down for desc); Inactive headers show faint chevrons that highlight on hover; aria-sort attribute on table headers: 'ascending'/'descending' when active, 'none' when inactive; Severity sort uses semantic priority order (critical=0, important=1, moderate=2, low=3); Approval status sort respects priority: pending, deferred, approved, declined; Release date sorts chronologically (oldest first ascending); Patch title sorts alphabetically case-insensitive; Page resets to 1 on any sort change; No console errors during sort operations
  - ⚠️ Watch: Empty result set after sorting: shows 'No patches found', not a silent failure; Dark theme: chevron icons have sufficient contrast against header background; Source and OS columns sort client-side only (server-side sort not yet wired); Severity sort divergence: client-side uses semantic priority, API sorts alphabetically; Filtering AND sorting together: sorted results respect active filters; Repeated clicks toggle direction correctly; Very long patch titles: do not break the header alignment
- [ ] **Ring-level auto-approval gate in Update Ring form** — _Patch Management > Update Rings tab > Create/Edit Ring modal_
  - Steps: Navigate to sidebar > Patches → Click the 'Update Rings' tab → Click 'New Ring' button to open the ring creation form → Scroll to the 'Auto-approve patches' section (bordered box) → Verify the checkbox is initially unchecked and severity/deferral controls are hidden → Check the 'Auto-approve patches' checkbox → Observe severity chip buttons appear: Critical, Important, Moderate, Low → Verify a 'Deferral (days after release)' input field appears → Click the 'Critical' severity chip to select it (should highlight with color) → Click the 'Important' severity chip to select it as well → Type '7' in the Deferral field → Uncheck one severity (e.g. Important) to deselect it → Fill in required fields (Ring name: Pilot, Order: 1) → Try to submit without selecting any severities (check auto-approve, uncheck all) → Verify validation error: 'Select at least one severity for auto-approval' → Select Critical again and submit the form → Verify the ring is created and the modal closes
  - ✅ Verify: Auto-approve section is always visible and toggleable; When unchecked: severity chips and deferral input are hidden; When checked: severity chips and deferral input are shown; Severity chips toggle their state on click (selected = colored, unselected = grey); Multiple severities can be selected simultaneously; Deferral field accepts 0-365 days; Form validation blocks submit if enabled without at least one severity selected; Error message appears as red text under the severity chips section; Selected state persists when editing an existing ring; No validation error when auto-approve is disabled; Form submission includes autoApprove with enabled, severities, and deferralDays fields
  - ⚠️ Watch: Empty severity list: validation should fail (fail-closed by schema); Deferral field validation: negative numbers rejected, max 365 enforced; Dark theme: severity chips have sufficient color contrast in both states; When editing a ring: auto-approve settings should pre-populate from existing data; Form reset: defaults should apply when opening form again; Category Rules section does not interfere with ring-level auto-approve; All toggles and chips are keyboard-focusable and usable with Enter/Space; Very long ring name: form layout does not break

## Setup & Misc — Guided Self-Hosted Setup, Profile Theming, Timezone, Warranty, Registration Toggle, Discovery Topology

- [ ] **Profile Theming & Font Selection** — _Settings > Profile > Theming section_
  - Steps: Navigate to /settings/profile → Scroll to the 'Theming' section (after account/avatar/password sections) → Under 'Theme': click Light, Dark, or System buttons to toggle preference → Under 'Interface density': click Comfortable, Compact, or Dense buttons → Under 'Font selection': click 'Breeze default' (Plus Jakarta Sans) or 'System' (OS interface font) → Observe the UI updates immediately after each selection → Refresh the page to verify preferences persisted → Switch to a different organization tab and return to verify state is preserved
  - ✅ Verify: Selected theme button shows 'border-primary bg-primary/10' styling with checkmark; Light theme: white/light backgrounds visible immediately; Dark theme: dark backgrounds + light text visible immediately; System theme: matches OS dark mode preference; Density changes: Comfortable has more padding, Compact/Dense reduces spacing visibly; Font changes: interface font visually switches (serif to sans-serif or system font); Success toast appears: 'Theming preferences saved.'; Preferences remain after page reload; All three settings are independent (changing one doesn't reset others)
  - ⚠️ Watch: Race condition: simultaneous changes before first save completes (test with slow network); Toast message timing: verify success/error toast appears consistently at expected location; Dark theme contrast: check primary button text contrast in dark mode (theme toggle buttons use border-primary); Font rendering: verify custom fonts load in dense mode without layout shift; Missing preference: when user pref empty, verify fallback to system/localStorage value; Disabled buttons during save: check all three fieldsets disable during isSavingAppearance=true; No silent failures: verify network errors show in error state box (red bg with destructive text color); Empty state: when no preferences set, buttons show default system values
- [ ] **Partner Timezone Default & Regional Settings** — _Settings > Partner > Regional tab_
  - Steps: Navigate to /settings/partner (requires partner-scoped JWT) → Click the 'Regional' tab (second tab after 'Company') → In 'Regional Settings' section, open the 'Timezone' dropdown → Select a timezone (e.g., 'America/New_York', 'Europe/London', 'Asia/Tokyo', 'UTC') → Observe the selection updates the dropdown label → Change 'Date Format' dropdown (MM/DD/YYYY, DD/MM/YYYY, YYYY-MM-DD) → Toggle 'Time Format' radio buttons (12-hour vs 24-hour) → Click 'Save Settings' button at top-right → Verify the save toast and refresh the page to confirm persistence
  - ✅ Verify: Timezone dropdown correctly shows selected value (no validation errors); Date format dropdown saves and reflects choice on reload; Time format radio buttons show selected state and persist; Save button text changes to 'Saving...' with spinner during POST; Success toast: 'Partner settings saved'; After save, newly created sites inherit this timezone from /orgs/partners/me endpoint (verified in OrganizationsPage.tsx line 142); First-class partner.timezone column is used as source-of-truth over legacy JSONB settings.timezone (verified in PartnerSettingsPage.tsx line 179)
  - ⚠️ Watch: Missing timezone load: partner.timezone not populated from DB (verify fetchPartner sets correct value); Timezone dropdown empty: check TIMEZONES array populated with all 15+ zones; Save without changes: clicking save when no field changed should still succeed with same toast; Offline fallback: if /config fails during site creation, new sites should still get timezone (UTC default in SiteForm); Legacy data migration: existing partners with only JSONB-stored timezone should read correctly from settings.timezone; Dark theme: verify date/time format selects are readable in dark mode; Lockout from IP allowlist: if IP allowlist is set, verify lockout confirmation dialog appears before save; Partial save failure: if timezone saves but date format doesn't, check error handling
- [ ] **Self-Hosted Guided Setup Wizard** — _/setup route (shown only when user.requiresSetup=true)_
  - Steps: As a new partner user, navigate to /setup (or login redirects here automatically) → Verify 'Account' step (step 0/3) displays with name/avatar fields → Fill in account details and proceed to step 1 → Verify 'Organization' step displays with org name, type, status fields → Create an organization (POST /orgs/organizations) → Verify 'Install Agent' step (step 2) displays with enrollment key download → Follow enrollment flow or click 'Back to Organization' to return → Complete setup then redirects to / → Test 'Skip setup — I'll configure this later' button on Account step (step 0)
  - ✅ Verify: SetupStepper component renders 3 steps: Account, Organization, Install Agent; Current step indicator highlights active tab and shows correct step content; Account step: name field required (min 2 chars per profileSchema); Organization step: org name/type required; Site/enrollment data persists to localStorage (SETUP_ORG_KEY, SETUP_SITE_KEY); Clicking step in stepper advances/regresses (onStepClick handler); Skip button calls /system/setup-complete and clears localStorage, redirects to /; User.requiresSetup=false on backend after completing setup; After completion, /setup redirects to / (requiresSetup guard in useEffect)
  - ⚠️ Watch: Auth guard: unauthenticated users redirected to /login (checkSetup effect); Already-completed users: if requiresSetup=false, redirect to / (don't show wizard); Missing org/site data on step 2: if SETUP_ORG_KEY empty, show fallback UI or error; localStorage unavailable: try/catch blocks handle quota exceeded in browser; Race condition: rapid step clicks before prior step saves; No backward nav from step 2: EnrollDeviceStep only has 'Back' button, not step clicks (onStepClick disabled after step 2); Skip on non-Account step: skip button only visible on step 0 (conditional render at line 171-180); Network failure: /system/setup-complete POST fails silently with console.warn (no user-facing error); Hydration mismatch: isHydrated delay (100ms) avoids SSR mismatch in localStorage access
- [ ] **Registration Toggle (Runtime Configuration)** — _Login > Register Partner link (conditionally shown), /auth/register-partner route_
  - Steps: Navigate to /auth/register (or /register-partner) → Verify 'Register as partner' form is visible OR 'Registration disabled' message appears → Check /config endpoint for registration.enabled flag (admin can toggle via ENV_ENABLE_REGISTRATION) → If enabled: fill registration form (company name, email, password, terms) → If disabled: verify form is hidden and redirect to /login?reason=registration-disabled occurs
  - ✅ Verify: PartnerRegisterPage checks useRegistrationGate() hook (enabled + loaded); If gateLoaded=false: render nothing (don't flash form while /config loads); If loaded=true, enabled=true: PartnerRegisterForm renders fully; If loaded=true, enabled=false: useEffect redirects to /login?reason=registration-disabled; useFeaturesStore.load() fetches /config on mount (runs once per session); /config response includes registration.enabled boolean; Default: registration.enabled=false (safe-closed unless explicitly enabled); Form submission calls apiRegisterPartner (POST /auth/register-partner)
  - ⚠️ Watch: Flash of form before /config loads: verify conditional render (gateLoaded check prevents render); Silent disable: if /config request fails, registration defaults to false (safe failure); Race condition: user navigates to /register before /config completes (gateLoaded=false → render null → prevent form submission); localStorage not available: featuresStore works without localStorage (Zustand fallback); Env var missing: ENABLE_REGISTRATION undefined → registration.enabled=false in API config.ts; Multiple concurrent /config calls: featuresStore.load() uses get().loaded guard (only fetches once); Server-side enforcement: /auth/register-partner endpoint re-checks ENABLE_REGISTRATION (client-side gate mirrors server check); Token refresh: after registration, new user gets JWT with correct scope; Deep-link during disable: user has /register-partner in URL → redirect to /login occurs
- [ ] **Warranty Alert Gating & AppleCare Subscription Display** — _Device Detail page > Warranty Information card (compact or full view)_
  - Steps: Navigate to /devices/{deviceId} (or device detail page) → Scroll to 'Warranty Information' card (if device has warranty data) → Observe warranty status badge: Active, Expiring, Expired, Unknown, or 'AppleCare subscription' → For AppleCare subscriptions: verify 'Renews {date}' label instead of 'Expires {date}' → Click 'Refresh' button to manually sync warranty (POST /devices/{deviceId}/warranty/refresh) → Verify last-sync timestamp ('Last checked: {timeAgo}') → Check data source label ('Agent (macOS plist)' or 'Vendor API') → View full warranty entitlements table if available
  - ✅ Verify: AppleCare subscription status shows as 'subscription_active' with success badge color (bg-success/15); End date labeled 'Renews' for subscriptions, 'Expires' for non-renewing warranties (isSubscription check); Warranty status config includes new 'subscription_active' key with correct color; Alert gating: warranty alerts only show if alert policy is assigned to the device (backend enforcement in #1344); Rolling-expiry suppression: AppleCare renewal dates don't trigger false 'expiring soon' alerts; Compact view: shows status badge + manufacturer + renewal date only; Full view: shows grid with Manufacturer, Serial, Start, End; plus entitlements table; Error state: 'Couldn't load warranty' with Retry button (distinct from 'No warranty information'); Empty data: device with no warranty shows 'No warranty information' in compact view (null check)
  - ⚠️ Watch: isSubscription flag missing: verify warranty.isSubscription or warranty.status='subscription_active' correctly identifies renewals; End date label wrong: if subscription has 'Expires' instead of 'Renews', logic error in isSubscription calculation; No refresh endpoint: POST /devices/{deviceId}/warranty/refresh returns success but data not updated (3s delay then re-fetch may miss update); Stale cache: GET /devices/{deviceId}/warranty returns cached data even after refresh requested; Serial number redaction: verify serial numbers are shown (no over-redaction from #1344 changes); Entitlements empty: if warranty.entitlements is undefined or [], table should not render; Source label mismatch: 'agent_plist' → 'Agent (macOS plist)', unknown source → raw string; lastSyncError legacy: pre-v0.13.9 stored 'No configured provider...' in lastSyncError; verify migration handled or removed; Fetch failure gracefully: if /devices/{deviceId}/warranty 403s, error state shows Retry (not silent failure); Dark theme: ensure text contrast on status badges in dark mode (currently using semantic colors)
- [ ] **Network Discovery: Honest Subnet-Grouped Topology View** — _Monitoring > Network Discovery > Topology Map visualization (SVG-based)_
  - Steps: Navigate to /discovery → Find the 'Network Topology' section or main topology map → Verify nodes render as circles (one per device) → Check subnet legend at bottom (data-testid='topology-subnet-legend') → Verify no synthetic edges between nodes (unless API returns actual measured edges) → Observe 'Subnet groups' label: '10.0.2.0/24 (2 hosts)', '192.168.0.0/16 (1 host)' → Verify CIDR mask applied correctly (e.g., /16 groups different /24 subnets correctly) → Read adjacency note: 'Links shown only when real adjacency is measured' (data-testid='topology-adjacency-note')
  - ✅ Verify: Nodes render with count matching API response (container.querySelectorAll('.nodes circle').length); Edges only render when API returns non-empty edges array (container.querySelectorAll('.links line') empty if no edges); Subnet grouping: nodes grouped by real subnet from API (subnets array), NOT by first 3 octets; /16 mask applied correctly: IPs .4.x and .9.x in same /16 group together (172.16.0.0/16 test case); Subnet legend shows all subnets from API and host counts per group; Adjacency note visible when edges=[]: 'shown only when real adjacency is measured' (matches regex /shown only when real adjacency is measured/i); SVG renders with data-testid='topology-svg'; Nodes have type labels (workstation, server, printer, router) and status (online/offline); No fabricated star-topology: old PR #1343 removed synthetic edges
  - ⚠️ Watch: Empty edges but note not shown: verify adjacency-note render (TestId='topology-adjacency-note'); Synthetic edges still drawn: PR #1343 should remove old star-topology code (check for missing nodes → gateway edges); Wrong CIDR grouping: subnets field not parsed or mask incorrect (old code sliced 3 octets; new code uses CIDR mask); Subnet legend missing: if topology-subnet-legend div not rendered, legend hidden; Host count wrong: if node not assigned to correct subnet group, count off by 1; Edge lines with no nodes: if API returns edges but missing corresponding node IDs, rendering error; Topology fetch failure: /topology endpoint 404 or 500 (error state handling); D3.js layout issues: nodes overlapping or off-screen if force simulation not initialized; SVG height class wrong: heightPxClass(height) doesn't apply height properly (h-96 not added); Node tooltip missing: hover should show node details (ip, type, status), verify no console errors
- [ ] **Organization Auto-Select from URL Hash & Partner Timezone Pre-Selection** — _Settings > Organizations page (/settings/organizations#{orgId})_
  - Steps: Navigate to /settings/organizations → Verify organizations list loads → Open browser devtools and manually set URL hash: /settings/organizations#org-uuid-here → Verify that organization with matching ID auto-selects → Check that sites for the selected org load automatically → Click a different org in the list (hash updates via window.location.hash = org.id) → In Sites section, click 'Add Site' button → Observe timezone field is pre-selected to partner's timezone (if set) → Verify timezone defaults to 'UTC' if no partner default configured → Refresh page with hash in URL to verify org and timezone state persists
  - ✅ Verify: initialOrgId extracted from window.location.hash on mount (line 44-47); selectedOrg matches initialOrgId when organizations array loaded (line 155-159); sites fetch triggered when selectedOrg changes (line 162-167); Hash updates when user clicks org (line 187: window.location.hash = org.id); URL is bookmarkable: /settings/organizations#org-123 always loads org-123; Org selector highlights selected org with visual feedback; Sites list updates immediately when org selection changes; partnerTimezone loaded from /orgs/partners/me on OrganizationsPage mount (line 135-151); SiteForm initial timezone value uses partnerTimezone if available, else 'UTC'; Timezone dropdown shows selected value in form
  - ⚠️ Watch: Hash extraction timing: initialOrgId read before DOM hydration (potential SSR/client mismatch); Org not found: hash contains invalid ID → selectedOrg stays null, sites list empty (no error toast); Hash collision: two orgs with same ID (impossible if UUIDs, but verify no race); Missing orgs: if organizations fetch 401s, hash is ignored; Sites fetch race: rapid org switches before prior sites fetch completes; Back/forward navigation: verify popstate event updates currentPath (sidebar nav tracking) + hash reconciliation; Empty hash: /settings/organizations (no #) → selectedOrg null, first org not auto-selected; Drag-reorder: if org is reordered, hash still points to correct org (ID-based, not index-based); Fetch failure: /orgs/partners/me fails → partnerTimezone stays undefined (best-effort, line 145); Timezone undefined: if data?.settings?.timezone missing, no error thrown (silent fallback to UTC); Cancelled fetch: if user navigates away, cancelled flag prevents stale setState (line 148-150); Default 'UTC': verify new sites get 'UTC' when partner timezone not configured
