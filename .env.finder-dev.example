# Template for .env.finder-dev (gitignored — never commit real values).
#
# Copy this file to .env.finder-dev and fill in real values:
#   cp .env.finder-dev.example .env.finder-dev
#
# Referenced by docker-compose.finder-dev.override.yml's `env_file:` entry
# for the `api` service — see extensions/README.md for the override pattern.

# Voyage AI embeddings key, used by the workspace content-preview extension's
# vector-embedding step. Get one at https://dash.voyageai.com.
VOYAGE_API_KEY=changeme
