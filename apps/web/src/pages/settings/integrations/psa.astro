---
// Integration config consolidated into the /integrations hub (tab-based UI).
// Keep this route as a permanent alias so existing bookmarks / in-app links land
// on the right tab instead of an orphaned standalone page.
return Astro.redirect('/integrations#psa', 301);
---
